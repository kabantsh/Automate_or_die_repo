قبل ما نتكلم عن ال Passwordless authentication لازم ناخد خلفية بسيطة عن التشفير بي المفتاح العام و المفتاح الخاص (public-private encryption) الفكرة إن في مفتاحين:
 - المفتاح العام (🔒 Public Key): ده المفتاح البتستخدمه عشان تشفر البيانات و دا بكون قاعد في العميل (Client).
 - المفتاح الخاص (🔐Private Key): ده المفتاح اللي لازم يبقى سري ومحفوظ مكان آمن و دا بكون قاعد في الخادم (Server).
    - المفتاحين ديل لازم يجو مع بعض (وفي مفتاح فيهم بيقفل بس و مفتاح بيفتح بس), يعني ما ينفع تفتح باب الشارع بي مفتاح الدولاب.

شوف، الـ Public Key Authentication دي حاجة مهمة جدًا في الـ IT.

1. التشفير اللي بيتعمل بيها الـ Public Key:
    - الـ Public Key Authentication بتستخدم خوارزميات تشفير زي RSA و DSA.

2. **الـ Key Pair - الـ Public والـ Private:**
    - يلا انت حتعمل زوج مفاتيح بإستخدام أدوات زي `ssh-keygen`و المفاتيح ديل بتستخدمهم للدخول للجهاز (بدل الباسورد)
 - الزوج ده بيتكون من:
        - **الـ Public Key**: ده اللي بيتم مشاركته مع الـ SSH server. أي حد عنده الـ Public Key ده بيقدر يشفر بيانات.
        - **الـ Private Key**: ده اللي لازم يبقى سري ومحفوظ عند الشخص نفسه. اللي عنده الـ Private Key ده بيقدر يثبت هويته.
    - الـ Private Key لازم يتعامل معاه بحذر وميتمشيش لأي حد تاني.

3. **كيفية إعداد الـ Public Key Authentication للـ SSH:**
    - الخطوات:
        1. إنشاء زوج المفاتيح (Key Pair): الشخص بيعمل لنفسه زوج المفاتيح (عادة بـ `ssh-keygen`).
        2. وضع المفتاح في الخادم (Server): الـ SSH server بيستقبل الـ Public Key وبيحفظه في ملف `authorized_keys`.
        3. المصادقة: لما الشخص يحاول يدخل على الـ server، الـ server بيتحقق إن الـ Private Key متطابق مع الـ Public Key المسجل.

الـ Public Key Authentication بتزيد الأمان وبتسمح بدخول آمن بدون كلمات مرور مع إمكانية استخدامها في الأتمتة داخل الشبكات الشركات و طبعا هي الأساس لي 